// Minified satellite.js v5.0.1 (จำลองบางส่วนสำหรับการใช้งาน)
var satellite = (function () {
    function twoline2satrec(line1, line2) { return {}; }
    function propagate(satrec, date) { return { position: { x: 1, y: 2, z: 3 } }; }
    function eciToGeodetic(pos, gmst) { return { longitude: 1, latitude: 1, height: 500 }; }
    function degreesLat(radians) { return radians * 180 / Math.PI; }
    function degreesLong(radians) { return radians * 180 / Math.PI; }
    function gstime(date) { return 1.0; }

    return {
        twoline2satrec,
        propagate,
        eciToGeodetic,
        degreesLat,
        degreesLong,
        gstime
    };
})();